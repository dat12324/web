extends ../../layout/default.pug
include ../../mixin/box-head.pug
include ../../mixin/product-layout.pug
block main
    .container.my-3
        .row
            .col-12
                +box-head(pageTitle)
        .row
            if(products.length === 0)
                .col-12
                    p Chưa có sản phẩm nào phù hợp với từ khóa tìm kiếm.
            else
            +product-layout(products)