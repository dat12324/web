.admin-sidebar
	.sidebar-brand
	nav
		ul.nav
			if(role.permissions.includes('products_view'))
				li
					a(href='/admin/products') Sản phẩm
			if(role.permissions.includes('products-category_view'))
				li
					a(href=`${preFixAdmin}/product-category`) Danh mục sản phẩm
			if(role.permissions.includes('roles_view'))
				li
					a(href=`${preFixAdmin}/roles`) Vai trò và quyền
			if(role.permissions.includes('accounts_view'))
				li
					a(href=`${preFixAdmin}/accounts`) Tài khoản người dùng
			if(role.permissions.includes('permissions_view'))
				li
					a(href=`${preFixAdmin}/roles/permission`) Phan quyền
